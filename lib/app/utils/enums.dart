// ignore_for_file: constant_identifier_names

enum StatusToPlay { GENRE, PLAYLIST, ARTIST, SINGLE }

enum Status { IDLE, LOADING, SUCCESS, ERROR }

enum StatusDownload { DOWNLOAD, DELETE }

enum StatusDownloadFrom { HOME, DASBOARD }

enum StatusInitHomeFrom { HOMEINIT, ITEM }

enum Availability { loading, available, unavailable }
